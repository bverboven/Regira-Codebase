namespace Regira.Security.Authentication.Web.Constants;

public static class RouteNames
{
    public const string Authenticate = nameof(Authenticate);
    public const string ConfirmEmail = nameof(ConfirmEmail);
}